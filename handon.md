# เอกสารส่งมอบและวิธีใช้ MuMu Pattern Studio

อัปเดตล่าสุด: 23 กรกฎาคม 2026
จอเกมเป้าหมาย: **1280×720**

## Pre Sync แบบรอสุ่มทักษะจริง

1. อัดหรือจัด Event ที่ใช้เปิดหน้า Boost, กดสุ่ม และ Tap กด Play ตัวสุดท้ายตามปกติ
2. เลือก Tap กด Play ตัวสุดท้ายแล้วเปลี่ยน Type เป็น `adaptive_wait`; ระบบจะแยก Tap Play เดิมกลับมาเป็น Event สุดท้ายให้อัตโนมัติ
3. ใส่จุดตรวจ Stop X/Y และตรวจเวลา Adaptive ให้อยู่ในจังหวะที่ปุ่ม Stop ปรากฏแล้ว
4. ตั้ง “หมดเวลา” เป็น `0` เพื่อรอจนสุ่มได้จริง ไม่ว่าดวงช้าหรือเร็ว
5. บันทึกแล้วกด Play มาโคร เมื่อ Stop หาย ระบบจะยิง Tap Play ตัวสุดท้ายทันที จากนั้นใช้ Pre Sync Extra และเข้า Auto Sync ตามค่าปัจจุบัน

ถ้าระบบกด Play เร็วไป ให้เพิ่ม “ต้องเปลี่ยนติดกัน (เฟรม)” เป็น 3 หรือเพิ่ม “ความต่างภาพ”; ถ้าไม่ยอมกด ให้ลด “ความต่างภาพ” ทีละน้อย หรือขยับจุดตรวจให้อยู่กลางปุ่ม Stop หลีกเลี่ยงแถบโหลดที่เคลื่อนไหว กด F8 เพื่อหยุดได้ทุกเวลา

## แก้ Event และลบ Post-game หลายรายการ

- คลิกเส้น Event บน Timeline เพื่อให้ระบบเปิด/เลื่อนไปยังแถวเดียวกันในตาราง
- กด “เลือก Post-game ทั้งหมด” แล้ว Ctrl+คลิกรายการที่ต้องการเก็บออกจาก selection
- กด “ลบที่เลือก” และตรวจจำนวนในหน้าต่างยืนยันก่อนลบ
- Safe Zone จะเปลี่ยน Required เป็น Safe Random เมื่อครอบ และเปลี่ยน Event ที่ระบบแปลงไว้กลับเป็น Required เดิมเมื่อย้าย Zone ออก

## เปิดโปรแกรม

1. เปิด MuMu ให้เข้า Android เรียบร้อย
2. ดับเบิลคลิก `START_HERE.bat`
3. รอหน้าต่าง **MuMu Pattern Studio** เปิด

Launcher ใช้ข้อความ ASCII ทั้งหมด จึงไม่ควรพบ `'��' is not recognized` อีก หากเปิดไม่ได้ให้ใช้ virtual environment ของโปรเจกต์:

```powershell
cd "โฟลเดอร์ที่ clone โปรเจกต์ไว้"
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe app.py
```

## วิธีอัดบน MuMu จริง

1. กด **ค้นหา ADB และเชื่อมต่ออัตโนมัติ**
2. ต้องเห็น serial พร้อม Resolution `1280x720`
3. กด **ใช้ค่าจากภาพนี้**
4. ก่อนอัด ทดลองคลิกใน MuMu แล้วกลับมาดู `Mouse recorder` ต้องเห็นพิกัดและคำว่า `พร้อม`
5. กด **เริ่มอัด F6** แถบจะแสดง `● PRE-SYNC` และเริ่มเก็บ Event ทันที
6. เล่นบนหน้าต่าง **MuMu จริง** การกดก่อนพบ Pause จะเก็บเป็น `pre_sync`
7. เมื่อ Auto Sync สำเร็จหรือกด `F9` แถบจะเปลี่ยนเป็น `● REC`; Event หลังจากนี้เป็น `synced`
8. เล่นจนจบเกม เมื่อระบบเห็น `XP` ต่อเนื่องและปุ่ม Pause หาย แถบจะเปลี่ยนเป็น `● POST-GAME`
9. หลังขึ้น `POST-GAME` ให้ใช้เมาส์คลิก `OK` หรือปุ่มเมนูต่อได้ โปรแกรมจะบันทึกเฉพาะ Tap/Hold; J/K และ Jump/Slide จะถูกปิดอัตโนมัติ
10. ดู `Events/Jump/Slide/Tap/Hold/ก่อน Sync/หลังจบ` ให้เพิ่มตรงกับการเล่น และดู `Jump detector` ว่าจับเป็น Single/Double ถูกต้อง
11. กด `F7` เพื่อหยุดอัด หน้าต่างใหม่จะให้เลือก **สร้าง Pattern ใหม่** หรือ **บันทึกทับ Pattern ที่มีอยู่**; โปรแกรมจะยังไม่เขียนทับอะไรจนกว่าจะกดบันทึก

ระหว่างอัด บรรทัด Mouse recorder ต้องขึ้น `พบ MuMu (x,y) • กำลังบันทึก` การกดใกล้ Jump `(160,635)` หรือ Slide `(1115,635)` จะเก็บเป็น Jump/Slide ตำแหน่งอื่นเก็บเป็น Tap/Hold พร้อมพิกัด

### ปุ่ม J และ Double Jump

- โปรแกรมอ่าน J/K ทั้งจากตัวอักษรและ Windows VK 74/75 จึงรองรับตอน MuMu เป็นหน้าต่าง active
- เมื่อ MuMu active โปรแกรม **บันทึกอย่างเดียว** และปล่อยให้ keymap ของ MuMu กดเกม เพื่อลดการกดซ้ำจาก ADB
- J ทุกครั้งจะเป็น Jump Event แยกกัน การกด J สองครั้งหรือแตะ Jump สองครั้งจะไม่ถูกรวมเป็น Hold
- ถ้า Jump สอง Event ใน phase เดียวกันห่างกันไม่เกิน 650 ms บรรทัด `Jump detector` จะแสดง `DOUBLE JUMP`, จำนวน 2 Events และค่า gap จริง
- หลังปล่อย J ต้องเห็น `พร้อมรับ Jump ถัดไป`; บัคเดิมที่จับ J ได้เพียงครั้งแรกแก้แล้ว

## Preview

ปุ่มเดิมเปลี่ยนเป็น **เปิด Preview (ไม่ใช้เล่น)** เพราะ Capture ผ่าน ADB ไม่ถึง 25 FPS อย่างเสถียร Preview มีไว้ตรวจภาพ/พิกัดเท่านั้น การเล่นและอัดให้ทำบน MuMu จริงซึ่งลื่นตาม FPS ของ Emulator

## Auto Sync หลายด่าน

- ระบบไม่เทียบภาพเต็มจอ ไม่สนพื้นหลังเทศกาล ฉาก คุกกี้ หรือข้อความที่เปลี่ยนไป
- ช่อง **Pause ของ Stage** เป็น Dropdown สำหรับเลือกหน้าตาปุ่ม Pause ของแต่ละด่าน; ค่าเริ่มต้นยังใช้กับด่านที่ปุ่มเหมือนเดิม
- แต่ละ Pattern จำชื่อ Pause Stage ที่เลือก พร้อม template/ROI ของตัวเอง และตรวจเฉพาะกรอบเล็กรอบไอคอน Pause
- เมื่อ MuMu อยู่ด้านหน้า ระบบ Capture เฉพาะ Pause ROI จากหน้าต่างเกมโดยตรง ไม่ Capture เต็มจอผ่าน ADB
- ค่า Fast ROI คือ `0.82 / 20 ms / 2 เฟรม`; `20 ms` เป็นรอบตรวจเป้าหมาย ไม่ใช่คำรับประกัน latency ของทุกแหล่งภาพ
- Threshold ช่องเดียวควบคุมทุกเงื่อนไขจริง: `รวม = Threshold`, `เส้น = Threshold - 0.06`, `โครง = Threshold - 0.10` เช่นตั้ง `0.82` จะใช้จริง `0.82 / 0.76 / 0.72`; ADB ไม่มีเกณฑ์ลับที่สูงกว่าค่านี้อีก
- ระบบจำเวลา **เริ่ม Capture ของเฟรมแรกที่ตรง** แล้วคำนวณ `จุดเริ่ม Pattern = เฟรม Pause แรก + Delay` เหมือนกันทั้งตอนอัดและตอนเล่น จึงไม่บวก capture latency ซ้ำ
- ปุ่ม **คงที่ • Delay 300 ms (แนะนำ)** ใช้จุดเริ่มคงที่ 300 ms หลังเฟรมแรก แม้ Detector จบใน 40 หรือ 180 ms; ถ้าต้องการเร็วสุดใช้ **เร็วสุด • Delay 0 ms** และปรับ Delay เองได้ `0–3000 ms`
- Pattern ใหม่เริ่มที่ Delay `300 ms`; Pattern เดิมไม่ถูกแก้ค่าอัตโนมัติ เพื่อรักษาจังหวะที่เคยตั้งไว้
- Pattern ที่อัดด้วย Delay `0 ms` ให้เล่นด้วย `0 ms` ต่อไป อย่ากด preset 300 ก่อนเล่นงานเก่า เพราะจะเลื่อน Event ทั้งชุด 300 ms
- ถ้า MuMu ไม่ได้อยู่ด้านหน้า Fast ROI จะพัก แต่ ADB Raw ยังตรวจต่ออัตโนมัติ; ควรกลับไปโฟกัส MuMu เพื่อให้เส้นทางเร็วทำงานด้วย
- MuMu แบบหน้าต่างสูง/แนวตั้งจะหา display child ก่อน แล้วคำนวณพื้นที่เกม 16:9 ภายใน content เพื่อไม่ให้ ROI เลื่อนเพราะ title bar/toolbar
- ระหว่าง `WAITING_FOR_AUTO_SYNC` ระบบแข่งสองทาง: Fast ROI ตรวจถี่เพื่อ latency ต่ำ และ ADB Raw native ตรวจต่อเนื่องเพื่อกู้กรณี Window ROI เปิดได้แต่พลาดเฟรม/พิกัด เส้นทางที่พบ Pause ก่อนเป็นผู้ชนะ
- Fast ROI ได้ช่วงนำคงที่ `80 ms` เพื่อไม่แย่งทรัพยากร จากนั้น ADB Raw จะเริ่มเสมอแม้ Fast ROI เปิดได้แต่ Match ไม่เจอ และหยุดเมื่อ Sync สำเร็จ
- เมื่อครบเวลาช่อง `ลองตรวจใหม่ทุก` ระบบ re-arm Detector อัตโนมัติ ไม่เปลี่ยนไปค้าง `รอ F9`; Loop จึงรอ Pause รอบใหม่ต่อได้จนกว่าจะพบหรือผู้ใช้กด F8
- Poll คำนวณแบบเวลาเริ่มเฟรมถึงเวลาเริ่มเฟรม และหลังเฟรมแรกผ่านจะยืนยันเฟรมสองแบบ burst `5 ms` แทนการรอเต็ม `20 ms`
- Fast ROI และ ADB fallback ใช้เกณฑ์จากช่อง Threshold ชุดเดียวกัน ภาพ gameplay ล่าสุดได้ `0.899/0.861/0.931` จึงผ่านค่ามาตรฐาน ขณะที่หน้าก่อน Pause เดิมยังถูกปฏิเสธ
- ค่า Threshold/Poll/จำนวนเฟรมของแต่ละ Pattern จะแสดงและใช้งานตรงตามไฟล์ ไม่มีการรีเซ็ตเงียบเป็น preset ตอนเปิด Pattern อีก
- ถ้าด่านใดไอคอน Pause เปลี่ยน ให้กด **สร้าง/อัปเดต Pause ของ Stage**, ตั้งชื่อ Stage แล้วเลื่อนกรอบคงที่ให้ครอบไอคอน จากนั้นเลือกกลับมาใช้ได้จาก Dropdown โดยไม่ต้องสร้างใหม่ทุกครั้ง
- กรอบ Pause ถูกล็อกเป็น `62×62 px` ทุก Stage และก่อนคำนวณคะแนนภาพปัจจุบันจะถูกทำให้มีขนาดตรงกับ Template เสมอ
- ปุ่ม **ทดสอบจริง 8 เฟรม (เปิด MuMu อัตโนมัติ)** ตรวจทั้งความเสถียร คะแนน latency และแหล่งภาพ แทนการสรุปจากภาพเดียว
- ห้ามทดสอบซ้อนระหว่างอัดหรือเล่น โปรแกรมจะแจ้งให้กด F7/F8 ก่อน เพื่อไม่ให้ Detector สองตัวแย่งกัน

วิธีทดสอบ Fast ROI ให้ถูกต้อง:

1. เลือก Pause Stage ให้ตรงกับด่าน แล้วเปิดเกมค้างหน้าที่เห็น Pause
2. หยุดการอัด/เล่นก่อน แล้วกด **ทดสอบจริง 8 เฟรม (เปิด MuMu อัตโนมัติ)**
3. โปรแกรมจะเปิด `Android Device-1` เองและนับเฉพาะ `MuMu Window ROI` จริง ไม่เอาภาพดำหรือ ADB มาปน
4. ดู `ผ่าน N/8` และค่า Capture median/max ประกอบกัน ไม่ตัดสินจากคะแนนภาพเดียว

หน้าต่างผลทดสอบจะแสดง `เกณฑ์ที่ใช้จริง` ครบทั้งรวม/เส้น/โครง ทุกครั้งที่เปลี่ยน Threshold แล้วกดทดสอบ ตัวเลขทั้งสามต้องเปลี่ยนตามทันที

ผล `0/8 • รวม 0.642` และ `0/8 • รวม 0.683` จากภาพร้านค้า/หน้าเตรียมที่แจ้งมาคือ **ผลถูกต้อง** เพราะสองหน้านั้นยังไม่มีปุ่ม Pause ส่วนภาพในด่านที่เห็น Pause จริงได้ `รวม 0.9366 / เส้น 0.9056 / โครง 0.9689` และผ่านเกณฑ์ `0.82 / 0.76 / 0.72`

หน้าทดสอบจะแจ้ง `ไม่พบ Pause ในภาพปัจจุบัน` แทนคำว่า `ควรปรับ ROI` เมื่อได้ 0/8 หากกำลังอยู่หน้าร้านค้าหรือก่อนเข้าด่านไม่ต้องแก้อะไร; ให้ปรับ ROI เฉพาะเมื่อในภาพมองเห็น Pause แต่ยังได้ 0/8

จากการวัด ADB Raw จริง 8 เฟรมบนเครื่องนี้ Capture median ประมาณ `275 ms` จึงใช้เป็น fallback เท่านั้น ไม่เรียกว่า Ultra Low Latency

วิธีตรวจรอบนี้ในเกมจริง:

1. กดเล่น Pattern แล้วกลับไปโฟกัส MuMu ก่อนหน้า Pause
2. หลังพบ Pause ดูแถบสถานะ: ควรขึ้น `MuMu Window ROI` และ `ชดเชย Capture + ยืนยัน N ms`
3. ถ้าขึ้น `ADB verification fallback` ให้เลือก **คงที่ • Delay 300 ms (แนะนำ)** เพื่อให้เวลาจบการตรวจที่แกว่งไม่เลื่อนจุดเริ่ม Pattern
4. เทียบ Jump แรกหลัง Pause กับรอบที่อัด หากยังเหลื่อม ให้ปรับช่อง `ดีเลย์หลังพบ Pause` ทีละ 20–50 ms แล้วใช้ค่าเดียวกันตอนอัดและเล่น

## ตรวจจบเกมด้วย XP

1. ค่าเริ่มต้นใช้กรอบ XP จากภาพ Result ที่ให้มา: ROI `(155,450,130×75)` บนจอ `1280×720`
2. ระบบ Capture หนึ่งภาพต่อรอบ แล้วตรวจเพียงสองกรอบ: `XP ต้องตรง` และ `Pause ต้องหาย`
3. ต้องผ่าน 3 เฟรมต่อเนื่อง ค่าเริ่มต้น Poll `700 ms` และเริ่มตรวจหลังเล่น `15 s` จึงเบากว่าการ OCR/สแกนทั้งจอ
4. ถ้า XP โผล่เร็วกว่าจังหวะที่อัดไว้ ระบบตัด Jump/Slide ที่เหลือทันที
5. ถ้า Pattern เดินช้าหรือเร็วและ XP โผล่หลัง Event เกมสุดท้าย ระบบยังรอ XP ได้อีก `45 s` ก่อนเล่น Tap/Hold หลังเกม
6. ถ้าหา XP ไม่พบ ระบบหยุดอย่างปลอดภัยและจะไม่ส่ง Tap หลังเกมไปผิดหน้า
7. ถ้าคะแนน XP ต่ำ ให้เปิดหน้า Result จริง → **Capture กรอบ XP เอง** → ลากกรอบเล็กเฉพาะคำว่า XP → **ทดสอบหน้า Result ตอนนี้**

### Surprise Card

1. เปิด **แก้ Surprise Card อัตโนมัติ** ในส่วนหลังจบเกม
2. ถ้าต้องการทดสอบเฉพาะหน้าที่เปิดค้าง ให้กด **ทดสอบ/เล่น Surprise Card ตอนนี้ (ไม่รัน Pattern)**; ระบบไม่เริ่ม Pattern, Sync, Result หรือ Post-game และกด `F8` เพื่อหยุดได้
3. เกมอาจสลับชื่อ Jumping/Sliding ได้ทั้ง Layout `5 ใบ / 1 คำตอบ` หรือ `6 ใบ / 2 คำตอบ`; ระบบดูจำนวนการ์ดและกลุ่มภาพต่าง ไม่เดาจากชื่อเกม
   - แบบ 6 ใบใช้การแบ่งกลุ่ม `4+2` และ cluster margin `1.8` เพื่อไม่ให้ตัวละครขนาดเล็กถูกปฏิเสธเพราะคะแนนภาพต่ำ
4. ค่าเริ่มต้นรอ `1000 ms` ก่อนใบแรก, เว้นระหว่างสองใบ `500–900 ms` และรอรอบถัดไป `1800–2200 ms`
5. ระบบต้องเห็นคำตอบตรงกัน 2 เฟรมก่อนคลิก และต้องเห็นภาพหรือตัวนับรอบเปลี่ยนก่อนเริ่มรอบต่อไป
6. ถ้าหน้าเดิมค้าง ระบบจะ **ไม่คลิกซ้ำ** และหยุดอย่างปลอดภัย
7. ผ่านครบ 3 รอบแล้วจะกลับไปรอ XP Result ก่อนเล่น Post-game Events

## เล่น Pattern

1. เปิด Library แล้วเลือก Pattern ที่ Events มากกว่า 0
2. กด **เล่น Pattern**
3. ระบบเล่น Event `pre_sync` ก่อน
4. จากนั้นจึงรอ Auto Sync หรือ `F9`
5. เมื่อ Sync แล้วจึงเล่น Event `synced`
6. ระหว่างเล่นระบบเฝ้า XP; เมื่อพบจะหยุดคำสั่งเกมและเปลี่ยนไปเล่น Event `post_game` ซึ่งรับเฉพาะ Tap/Hold
7. ถ้ายังไม่พบ XP หลัง Event เกมสุดท้าย HUD แสดง `รอหน้า Result`; ถ้าหมดเวลาจะหยุดโดยไม่ Tap
8. `F8` หยุดได้ทุกเวลา

### เพิ่มเวลาเฉพาะก่อน Auto Sync

- ที่ช่อง **เวลาก่อน Auto Sync** เลือก `fixed` เพื่อใช้ค่าเดิม หรือ `random` เพื่อสุ่ม Min–Max
- ตั้ง **ห่างอย่างน้อย** เช่น `50 ms` เพื่อกันค่า `300` แล้วตามด้วย `310`; ตั้ง **จำย้อนหลัง** เพื่อไม่ให้ใกล้หลายรอบล่าสุด
- Fixed `500` หมายถึง หลัง Event `pre_sync` ตัวสุดท้าย ระบบรอเพิ่ม 500 ms แล้วค่อยเริ่มหา Pause
- ค่านี้ไม่เลื่อนเวลา Event `synced`, ไม่เปลี่ยน Event `post_game` และไม่รวมกับ Delay ระหว่าง Loop
- ถ้าเปิด `Sync/รอบ` จะใช้ทุก Loop; ถ้าปิด จะใช้เฉพาะรอบแรกซึ่งเป็นรอบเดียวที่ทำ Auto Sync
- ค่าเก็บแยกใน Pattern และติดไปกับ Save/Load/Import/Export; Pattern เก่าจะได้ค่า `0` อัตโนมัติ

สาเหตุเดิมที่พบ Pause แล้วจบทันทีคือ Pattern มี 0 Events ปัจจุบันโปรแกรมป้องกันไม่ให้เล่น Pattern ว่างแล้ว

ระหว่างเล่น HUD แสดงแบบสดทุกประมาณ 0.1 วินาที:

- `PLAY เวลาปัจจุบัน / ความยาวช่วง`
- `ช่วง ก่อน Sync / รอ Auto Sync / หลัง Sync / พักก่อน Loop`
- `ช่วง รอหน้า Result / หลังจบเกม` เมื่อ Pattern มี Tap หลังเกม
- `รอบปัจจุบัน/จำนวนรอบ` หรือ `∞`
- `Event ปัจจุบัน/Events ทั้งหมด`, progress bar และเวลาที่เหลือ

## Timeline Safe Zone

1. เปิดแท็บ **Random และ Safe Zone (ขั้นสูง)**
2. เลือก Pattern จากช่องบนสุด แล้วกด **โหลด Pattern นี้**
3. Timeline มีสามเลน: สีส้ม `ก่อน Sync`, สีฟ้า `หลัง Sync` และสีเขียวอ่อน `หลังจบเกม`
4. ลากช่วงในเลน **หลัง Sync** แล้วกด **สร้าง Safe Zone (N Events)**
5. แก้ Zone เดิมโดยคลิกกรอบเขียว ลากขอบซ้าย/ขวา แล้วกด **อัปเดต Zone ที่เลือก**
6. ลบโดยคลิกกรอบเขียวหรือแถว Zone แล้วกด **ลบ Zone ที่เลือก**; ถ้ามี Safe Random ผูกอยู่ โปรแกรมจะแจ้งจำนวนและถามก่อนลบพร้อมกัน
7. แท็บเลื่อนแนวตั้งได้ ปุ่มลบบน Timeline ยังมองเห็นได้ที่หน้าต่างขั้นต่ำ `900×620`
8. ระหว่างเล่น Playhead จะเดินเฉพาะเลน phase ปัจจุบัน รีเซ็ตเวลาเมื่อเปลี่ยน phase และเลื่อน Timeline ตามอัตโนมัติ
9. ตาราง Event และ Safe Zone มีปุ่ม **ทำสำเนา**; สำเนาจะได้ ID/เวลาใหม่และเปิดให้ปรับก่อนบันทึก

ทดสอบบัคเดิมได้โดยลากคลุม Event มากกว่า 10 จุด: ข้อความจะแสดงตัวอย่าง 4 จุดกับ `และอีก N` ส่วนปุ่มสร้างต้องยังเห็นและกดได้

Safe Random ใช้ได้เฉพาะหลัง Sync เพื่อไม่ให้ขั้นตอนเริ่มเกมถูกสุ่ม Safe Zone ถูกเก็บแยกใน Pattern ปัจจุบัน และ Required Event ที่อยู่ใน Zone จะยังเป็น Required จนกว่าผู้ใช้เลือกเปลี่ยนเอง

Logic ความปลอดภัยของ Random: chance แบบ weight จะถูกแปลงเป็นเปอร์เซ็นต์รวม 100%, jitter ต้องไม่ออกนอก Zone, Slide ต้องจบภายใน Zone และต้องห่าง Required Event ตาม safety gap

ถ้า Pattern เก่าผิดกฎใหม่ โปรแกรมจะเปิด **โหมดซ่อม** และแสดง Event ที่ผิดเป็นแถวสีแดง ไม่ปิดกั้นทั้ง Pattern สามารถแก้/ลบ/ขยาย Zone และบันทึกความคืบหน้าได้ แม้ยังแก้ไม่ครบ โดยสำรองต้นฉบับเป็น `.json.repair.bak`; แต่จะ Play/Record ไม่ได้จนกว่า validation ผ่านทั้งหมด

## Loop Pattern

- `รอบ` = จำนวนรอบ
- `Loop จน F8` = เล่นต่อเนื่องไม่จำกัด
- `Delay ms` = เวลาพักหลังจบรอบก่อนเริ่มรอบใหม่
- `Sync/รอบ` เปิด = หา Pause ใหม่ทุก Loop
- `Sync/รอบ` ปิด = Sync เฉพาะรอบแรก

ค่า Loop ถูกบันทึกใน Pattern และติดไปกับ Save/Load/Import/Export

## Library

รองรับ Save, Load, Rename, Delete, Import และ Export พร้อมจำนวน Events, ความยาว Pattern, play count และเวลาที่เล่นล่าสุด Export ZIP จะรวมทั้ง Pause template และ Result XP template; Import ชื่อซ้ำจะต่อ `_2`, `_3`

### หลังอัดเสร็จบันทึกลง Pattern ไหน

1. กด `F7` แล้วงานอัดจะหยุดเป็น `DRAFT` ก่อน ไม่มีไฟล์ใดถูกเขียนทับทันที
2. ค่าเริ่มต้นคือ **สร้าง Pattern ใหม่** พร้อมชื่อแนะนำ เช่น `stage_01_take`
   - Pattern ใหม่จะเริ่มแบบ **Set Zero**: ไม่มี Safe Zone ติดมาจาก Pattern ต้นทาง และ Event ที่อัดเป็น Required ตามเดิม
3. ถ้าต้องการแทนที่งานเดิม ให้เลือก **บันทึกทับ Pattern ที่มีอยู่** แล้วเลือกชื่อจาก Dropdown
4. ถ้ายังไม่พร้อมตัดสินใจ กด **เก็บเป็นงานค้างก่อน**; กด `F7` อีกครั้งเพื่อเปิดหน้าต่างเลือกที่บันทึก
5. โปรแกรมจะไม่ให้เริ่มอัดรอบใหม่หรือปิดโปรแกรมโดยไม่เตือน หากยังมี Draft ค้างอยู่

หน้าหลักและหน้าตั้งค่ามีแถบเลื่อนเมื่อหน้าต่างเล็ก ตารางมี horizontal scrollbar และส่วนตั้งค่าขั้นสูงจะเรียงลงแนวตั้งอัตโนมัติเมื่อความกว้างต่ำกว่า 820 px

## ค่าของเครื่องนี้

- ADB: ให้โปรแกรมค้นหาอัตโนมัติหรือเลือก `adb.exe` ของ MuMu ด้วย Browse
- Serial หลัก: `127.0.0.1:16416`
- Serial ที่พบ: `127.0.0.1:5557`, `127.0.0.1:16416`, `emulator-5556`
- Resolution: `1280×720`
- Jump: `(160,635)`
- Slide: `(1115,635)`
- Pause ROI: `(1165,5,62×62)`
- Pause template: `templates/default_pause_1280x720.png`
- Auto Sync Fast ROI: Threshold `0.82`, Poll เป้าหมาย `20 ms`, ต่อเนื่อง `2` เฟรม, anchor จากเฟรมแรก
- Result XP ROI: `(155,450,130×75)`
- Result XP template: `templates/default_result_xp_1280x720.png`
- Result แนะนำ: Threshold `0.86`, Poll `700 ms`, ต่อเนื่อง `3` เฟรม, Pause ต่ำกว่า `0.80`

## ผลตรวจรับ

- Unit tests: **113/113 ผ่าน**
- Compile check: ผ่าน
- ADB/serial/resolution/Raw screenshot: ผ่านกับ MuMu จริง
- ตรวจจับ process และ viewport ของ `MuMuNxDevice.exe`: ผ่าน
- คลิก MuMu จริงแล้วแสดงพิกัด `(1048,476)`: ผ่าน
- อัดก่อน Auto Sync จาก MuMu จริง: ขึ้น `Events 1 / Tap 1 / ก่อน Sync 1`: ผ่าน
- Pre-Sync playback, Loop delay, Sync ครั้งแรก/ทุกรอบ: มี Unit test
- เวลาเพิ่มก่อน Sync: มี Unit testว่าหน่วงเฉพาะขอบเขต Sync, ไม่เลื่อน Event หลัง Sync และใช้เฉพาะรอบที่ทำ Sync
- ผลทดสอบ Auto Sync 8 เฟรม: มี Unit testสรุปคะแนน ความเสถียร latency และแหล่งภาพ
- Fast ROI กับ ADB Raw แข่งกันหลังช่วงนำ 80 ms, native path กู้ได้แม้ Fast ROI เปิดได้แต่ไม่ Match: มี Unit test
- Pause Stage Library, Default profile, การอัปเดตชื่อเดิม, path ปลอดภัย และบังคับ Crop 62×62: มี Unit test
- ภาพเทียบถูกปรับเป็นขนาดเดียวกับ Template และภาพดำ sentinel ไม่ถูกนับเป็น Fast ROI: มี Unit test
- Sentinel ไม่เข้า score/history/callback และ fallback แบบ Studio foreground จบได้ราว 305 ms ในการจำลอง Capture 170 ms: ผ่าน
- Threshold ผู้ใช้ขยับเกณฑ์รวม/เส้น/โครงจริง และจุด Sync คงที่ยึด Pause เฟรมแรกแม้เวลาประมวลผลต่างกัน: มี Unit test
- โหลด Pattern แล้วคงค่า Threshold/Poll ที่ผู้ใช้บันทึกไว้ ไม่บังคับกลับเป็น `0.82/20`: ตรวจ UI แล้ว
- Fast ROI เปิดได้แต่ไม่เคย Match จะถูก ADB Raw native กู้, Timeout re-arm เองหลายครั้ง และ F9/F8 ยังขัดจังหวะได้: มี Unit test
- จำลองด้วยภาพ Pause จริงโดยบังคับ Fast ROI ผิดตลอด: ADB Raw ได้ `0.9366`, Auto Sync สำเร็จราว `267 ms`, anchor อยู่ที่เวลาเริ่ม Capture และ Delay 300 ms ยังเหลือเวลารอราว `114 ms`: ผ่าน
- Playback HUD: มี Unit test ครบ phase, เวลา, Event progress และ Loop delay
- J/K char + VK และ J กดซ้ำหลังปล่อย: มี Unit test
- Single/Double Jump: ยืนยันว่าเก็บเป็น Event แยกและรักษา gap เดิม
- Timeline range, หลาย Event, เลือก Pattern, ส่งต่อ Zone ที่เลือก และลบ Zone พร้อม Safe Random: มี Unit test/UI check
- Playhead แยก phase, การเลื่อน Timeline, Required ใน Safe Zone, random weight และ Slide+jitter ไม่ออกนอก Zone: มี Unit test
- อัดใหม่จาก Pattern ที่มี Safe Zone แล้วยัง Finish/Save ได้โดย Event ที่อัดคงเป็น Required: มี Unit test
- Pattern ที่มี Event ผิดกฎเปิดโหมดซ่อมได้, แสดงแถวผิด, บันทึกความคืบหน้าแบบ atomic และกลับมา strict save ได้หลังแก้ครบ: มี Unit test/UI smoke test
- หน้าต่างย่อย Event/Safe Zone มี scrollbar; ล้อเมาส์และ high-resolution touchpad เลื่อนขึ้น/ลงได้เมื่อหน้าต่างเตี้ย: มี Unit test/UI smoke test
- Launcher ASCII: มี Unit test
- Pause false-positive: ภาพก่อน Pause ที่ให้มาได้ `รวม 0.686 / โครง 0.622 / เส้น 0.695` และถูกปฏิเสธเพราะโครงต่ำกว่า `0.72`
- Result reference: คำว่า XP ได้ `รวม 0.973 / โครง 0.976 / เส้น 0.958`; Pause บนหน้า Result ต่ำและผ่านเงื่อนไข `Pause หาย`
- Result early/late/timeout: มี Unit testว่าตัด Jump/Slide, รอ XP หลัง Event สุดท้าย และไม่ Tap เมื่อ timeout
- ZIP Import/Export: มี Unit testว่ารวมและคืนทั้ง Pause/XP template
- Low-latency ROI mapping, fast capture, first-frame anchor และ Player latency compensation: มี Unit test
- ADB/slow capture latency compensation: มี Unit testจำลอง Capture ช้าและยืนยันว่า anchor ย้อนถึงเวลาเริ่มถ่าย
- Portrait/letterbox viewport และ ADB verification fallback เมื่อ Fast ROI ผิดตำแหน่ง: มี Unit test
- MuMu จริง `127.0.0.1:16416` ยังเป็น `1280×720`; หน้าปัจจุบันที่ไม่มี Pause ได้ `รวม 0.6399 / เส้น 0.6579 / โครง 0.4892` และถูกปฏิเสธที่เกณฑ์ `0.82 / 0.76 / 0.72`: ผ่าน
- F7 มี New/Overwrite/เก็บ Draft/ไม่บันทึก โดยปุ่มทิ้งงานต้องยืนยันซ้ำ; ตรวจ compile/UI แล้ว

ยังต้องให้ผู้ใช้ทดสอบในเกม: อัดด่านจริง → ตรวจ Events → F7 → Load → เล่นกลับ หากผิดจังหวะให้ส่ง Pattern JSON รอบนั้นมา

หมายเหตุ Pattern `sliver_all_nofast_take_2` ปัจจุบันมี `pre_sync 2 / synced 31 / post_game 0` Events หาก Loop กลับหน้า Play ไม่ได้หลังแก้ Sync แล้ว ต้องอัด/ย้าย Tap ปุ่ม OK และกลับหน้าเลือกด่านให้เป็น phase `post_game`; ระบบ Auto Sync ไม่สามารถกดออกจากหน้า Result แทน Event ที่ไม่มีได้

## แก้ปัญหาเร็ว

- **PRE-SYNC นาน:** ระบบยังเก็บ Event อยู่ ถ้าต้องเริ่มเวลา synced ให้กด `F9`
- **Auto Sync ยังช้า:** ให้หน้าต่าง MuMu อยู่ด้านหน้า ถ้าสถานะขึ้น `ADB fallback` แปลว่ายังไม่ได้ใช้ fast ROI; ค่าแนะนำคือ `0.82 / 20 / 2`
- **อยากเริ่มตรวจ Pause ช้าลง แต่ไม่ขยับ timeline หลัง Sync:** ใส่ `เพิ่มเวลาก่อน Auto Sync` เช่น `500`
- **อยากให้ Pattern เริ่มหลังเห็น Pause แบบคงที่:** ใช้ปุ่ม `คงที่ • Delay 300 ms` หรือกรอก `ดีเลย์หลังพบ Pause`; ใช้ค่าเดียวกันตอนอัดและเล่น
- **ปรับ Threshold แล้วเหมือนไม่เปลี่ยน:** กดทดสอบ 8 เฟรมและดูบรรทัด `เกณฑ์ที่ใช้จริง`; เกณฑ์เส้น/โครงจะขยับตาม Threshold โดยไม่มีค่าแอบซ่อน
- **Auto Sync ไม่ทำงาน:** ดูข้อความ `เริ่มตรวจใหม่รอบ N` และแหล่งคะแนนดีที่สุด ระบบจะลองต่อเองโดยไม่ต้อง F9; กด F8 หากต้องการหยุด
- **Loop ค้างรอ F9:** ปิดโปรแกรมเก่าแล้วเปิดใหม่ รุ่นนี้ Timeout จะ re-arm อัตโนมัติทุก Loop
- **ขึ้น 0/8 และ 0.382 เท่ากันทุกเฟรม:** เป็นโปรแกรมเก่าที่นับภาพดำตอน MuMu ไม่อยู่ด้านหน้า ปิดโปรแกรมแล้วเปิด `START_HERE.bat` ใหม่ รุ่นนี้จะเปิด MuMu อัตโนมัติและไม่นับภาพดำ
- **Pause แต่ละ Stage ไม่เหมือนกัน:** เลือกจาก Dropdown `Pause ของ Stage`; ถ้ายังไม่มีให้กด `สร้าง/อัปเดต Pause ของ Stage` และตั้งชื่อใหม่
- **Sync ก่อนเห็น Pause:** เปิดแท็บตั้งค่าละเอียด กด `ใช้ Fast ROI`; ตรวจว่า ROI ครอบเฉพาะไอคอน Pause ไม่ติดพื้นหลัง
- **ด่านอื่นพื้นหลังต่างกัน:** ไม่ต้อง Capture ทั้งฉาก ระบบดูเฉพาะ Pause ROI; Capture ใหม่เฉพาะเมื่อ “ตัวไอคอน Pause” เปลี่ยนจริง
- **ไม่ขึ้น POST-GAME:** ค้างหน้า Result แล้วกด `ทดสอบหน้า Result ตอนนี้`; ถ้า XP ต่ำกว่า `0.86` ให้ Capture กรอบ XP ใหม่
- **ขึ้นรอหน้า Result แล้วหยุด:** ระบบ timeout เพื่อกัน Tap ผิดหน้า เพิ่มเวลารอได้ หรือ Capture XP ใหม่ ห้ามลด Threshold มากเกินไป
- **Events ไม่เพิ่ม:** ดู Mouse recorder ต้องขึ้นพิกัดและ `กำลังบันทึก`
- **J ได้แค่ครั้งเดียว:** ปิดโปรแกรมรุ่นเก่าแล้วเปิดใหม่ จากนั้นดู `Keyboard recorder` ต้องสลับ J DOWN/J UP และ Jump ต้องเพิ่มทุกครั้ง
- **Double Jump ไม่ขึ้น:** กด/แตะ Jump สองครั้งภายใน 650 ms แล้วดู `Jump detector`; ใน Event table ต้องมี Jump 2 แถว
- **Preview FPS ต่ำ:** เป็นข้อจำกัด ADB ไม่ใช้ Preview เล่น ให้เล่นบน MuMu จริง
- **F7 แล้วไม่อยากบันทึก:** กด **ไม่บันทึก (ทิ้งงานอัด)** แล้วกดยืนยัน; Pattern เดิมจะไม่ถูกเขียนทับ
- **มี Draft ค้าง:** กด `F7` เพื่อเปิดหน้าต่างเลือกที่บันทึกอีกครั้ง
- **Events 0:** เก็บ Draft ไว้ก่อนหรืออัดใหม่ และดูตัวนับก่อน F7
- **หยุดทันที:** กด `F8`

คู่มือเต็มอยู่ที่ `README_TH.md`
